apiVersion: v1 
kind: Pod 
metadata:
 name: demo-pod 
 labels:
  name: demo-app
  tier: app
spec:
 containers: 
 - name: demo-app-container 
   image: busybox 
   command: ['sh', '-c', 'echo hello KCAD & sleep 3600'] 
